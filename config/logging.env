# 知识库管理系统日志配置
# 可以通过修改这个文件或设置环境变量来调整日志行为

# 日志级别设置
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG - 显示所有日志，包括详细的调试信息
# INFO - 显示一般信息和更重要的日志
# WARNING - 只显示警告和错误
# ERROR - 只显示错误信息
# CRITICAL - 只显示严重错误
LOG_LEVEL=INFO

# 日志格式设置
# true - 使用简单格式（适合生产环境）
# false - 使用彩色格式（适合开发环境）
LOG_PLAIN=false

# 特定模块的日志级别控制
# 如果需要，可以为特定模块设置不同的日志级别
# SEARCH_LOG_LEVEL=DEBUG
# TREE_LOG_LEVEL=INFO
# OPTIMIZER_LOG_LEVEL=WARNING

# 使用说明：
# 1. 开发调试时，建议设置 LOG_LEVEL=DEBUG，LOG_PLAIN=false
# 2. 生产环境时，建议设置 LOG_LEVEL=INFO，LOG_PLAIN=true  
# 3. 如果想减少日志输出，可以设置 LOG_LEVEL=WARNING
# 4. 如果需要排查具体问题，可以临时设置 LOG_LEVEL=DEBUG 